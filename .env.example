# Copy to .env and fill in any secrets locally or in Vercel env vars
OPENAI_API_KEY=
# Use relative path in production web to avoid CORS/fetch issues.
PROXY_URL=/api/openai-proxy
PROXY_SEARCH_URL=/api/openai-search
